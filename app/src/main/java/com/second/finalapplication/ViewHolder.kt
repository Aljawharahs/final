package com.second.finalapplication

import android.util.Log
import android.view.View
import android.widget.TextView
import androidx.constraintlayout.widget.ConstraintLayout
import androidx.recyclerview.widget.RecyclerView

/*

class ViewHolder(view: View) : RecyclerView.ViewHolder(view) {
    private val name = view.findViewById<TextView>(R.id.name)
    private val descerption = view.findViewById<TextView>(R.id.)
    private val root = view.findViewById<ConstraintLayout>(R.id.root)

    fun bind(user: BrekfastMenu) {
        name.text = user.name
        descerption.text = user.email

        root.setOnClickListener {
            Log.d("TAG", "bind: CLICKED!!")
        }
    }
}
*/
